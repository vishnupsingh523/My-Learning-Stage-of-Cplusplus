#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;

    while(t--)
    {
        long long x, y;
        cin>>x>>y;

        if(x>1&&y>1)
        {
            long long minm = min(x,y);
            long long maxm = (x+y)-minm;
            
            if( __gcd(min, )))
            {

            }
        }      
    }
}