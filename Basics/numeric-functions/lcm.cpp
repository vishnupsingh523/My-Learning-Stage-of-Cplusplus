#include<bits/stdc++.h>

using namespace std;

int main(){

    // creating two variables a and b;
    int a = 2*3*5;
    int b = 2*3*3;

    // finding the gratest common divisor of a and b;
    cout<<"The lcm of a and b is : "<<__gcd(a,b);
}

